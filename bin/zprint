#!/usr/bin/env clj -Sdeps {:deps,{zprint,{:mvn/version,"0.4.9"}}}

(ns scripts.zprint
  (:require [zprint.core :as zp]))

(zp/zprint (slurp *in*) 80 {:parse-string-all? true
                            :parse {:interpose "\n\n"}
                            :map {:comma? false}})

(System/exit 0)
